addpath('classes/house-model')
house = House(10,10);
house = house.add_room("Kitchen", [1,1], [3,3]);
house = house.add_room("Hall", [4,1], [6,10]);

for i = 1:10
    for j = 1:10
        house.is_traversable(i,1)

    end
end


